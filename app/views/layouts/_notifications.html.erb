<% flash.each do |name, msg| -%>
  <% if msg.is_a?(String) %>
    <div data-alert class="alert-box radius <%= name == :notice ? "info" : "alert" %>">
      <%= content_tag :div, raw(msg) %>
    </div>
    <% end %>
  <% end %>